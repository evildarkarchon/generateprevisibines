## Deferred Items (Suggestions)

These are improvements that could be done later:
- Add structured logging with `tracing` crate
- Add configuration file support (TOML)
- Add progress indicators with `indicatif`